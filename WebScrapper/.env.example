# PostgreSQL / Supabase connection
DATABASE_URL=postgresql://user:password@localhost:5432/climbmy

# Individual connection parameters (alternative to DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=climbmy
DB_USER=user
DB_PASSWORD=password

# Export settings
CSV_OUTPUT_DIR=./output

